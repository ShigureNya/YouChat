<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppStyles" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textAllCaps">false</item>
    </style>


    <style name="AppStyles.Font" parent="AppStyles">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/mainTextColor</item>
        <item name="android:actionMenuTextAppearance">@style/MenuTextStyle</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />


    <style name="ToolbarPopupTheme" parent="@style/ThemeOverlay.AppCompat">
        <item name="android:colorBackground">@color/defaultBackgroundColor</item>
        <item name="android:textColor">@color/mainTextColor</item>
    </style>
    <!--菜单按钮字体大小样式-->
    <style name="MenuTextStyle">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/default_second_text_size</item>
    </style>
    <!--Navigation侧滑样式-->
    <style name="NavigationStyle">
        <item name="android:textSize">@dimen/navigation_item_size</item>
        <item name="android:textColor">@color/navigation_item_color</item>
    </style>
</resources>
